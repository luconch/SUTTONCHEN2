#include <stdio.h>
#include <math.h>

void SuttonChen(int a, int n, int m, float eps, float c);

int main(void)
{
    int a, n, m;
    float eps, c;
    
    // Parámetros para el níquel
    a = 3.52;
    eps = 0.015707;
    n = 9;
    m = 6;
    c = 39.432;

    SuttonChen(a, n, m, eps, c);
    return 0;
